terraform {
  backend "s3" {
    region         = "us-west-2"
    bucket         = "PREFIX-tf-state"
    key            = "PREFIX/cumulus/terraform.tfstate"
    dynamodb_table = "PREFIX-tf-locks"
  }
}
